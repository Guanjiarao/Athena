<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_chat_surface"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_chat_surface"
        android:orientation="vertical">

        <RelativeLayout
            android:id="@+id/toolbar_container"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:background="#FFFDFB"
            android:paddingStart="12dp"
            android:paddingTop="6dp"
            android:paddingEnd="12dp"
            android:paddingBottom="6dp">

            <ImageView
                android:id="@+id/btn_ai_back"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:background="@drawable/bg_ai_toolbar_button"
                android:contentDescription="back"
                android:padding="11dp"
                android:src="@drawable/ic_ai_back" />

            <TextView
                android:id="@+id/tv_toolbar_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:letterSpacing="0.01"
                android:text="@string/menu_sister"
                android:textColor="#2F2926"
                android:textSize="20sp" />

            <ImageView
                android:id="@+id/btn_open_drawer"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="@drawable/bg_ai_toolbar_button"
                android:contentDescription="menu"
                android:padding="11dp"
                android:src="@drawable/ic_ai_menu" />

        </RelativeLayout>

        <FrameLayout
            android:id="@+id/nav_host_fragment_content_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="264dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#FFFDFB"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_main"
        app:itemBackground="@drawable/drawer_item_selector"
        app:itemIconPadding="16dp"
        app:itemIconSize="32dp"
        app:itemIconTint="@null"
        app:itemTextAppearance="@style/AiMenuTextStyle"
        app:itemTextColor="#3D3632"
        app:itemVerticalPadding="12dp"
        app:menu="@menu/activity_ai_drawer" />

</androidx.drawerlayout.widget.DrawerLayout>
