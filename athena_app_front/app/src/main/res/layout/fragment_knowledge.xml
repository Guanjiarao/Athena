<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/athena_page_bg"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="104dp"
        android:layout_marginTop="48dp"
        android:paddingHorizontal="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:text="知识"
            android:textColor="@color/athena_text_primary"
            android:textSize="28sp" />

        <ImageButton
            android:id="@+id/knowledge_btn_search"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:background="@drawable/bg_athena_icon_button"
            android:clickable="true"
            android:contentDescription="AI 科普"
            android:focusable="true"
            android:padding="13dp"
            android:scaleType="centerInside"
            android:src="@drawable/science_ai"
            app:tint="@color/athena_text_primary" />

    </RelativeLayout>

    <FrameLayout
        android:id="@+id/knowledge_content_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
