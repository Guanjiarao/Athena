# Dispatch: Acceptance Window

你现在是验收窗口。

请使用 `triage-acceptance-window` 这个 skill 执行本轮任务。
先读：
- `docs/triage-control-room/active-task-brief.md`
- `docs/triage-control-room/development-report.md`

如需要审查长文件实现边界，请同时使用 `context-safe-editing`。

按 skill 的默认验收框架完成独立判断。
完成后写：
- `docs/triage-control-room/acceptance-report.md`

默认这是开发回合后的直接下一站。完成验收后，再由控制室决定下一刀 / 补刀 / test-followup / redesign。
