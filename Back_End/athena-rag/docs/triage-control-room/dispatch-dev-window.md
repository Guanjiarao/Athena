# Dispatch: Development Window

你现在是开发窗口。

请使用 `triage-dev-window` 这个 skill 执行本轮任务。
先读：
- `docs/triage-control-room/active-task-brief.md`

如涉及长文件、过载类、或需要分块安全编辑，请同时使用 `context-safe-editing`。

按 skill 的默认边界执行本轮单一结构 cut。
完成后写：
- `docs/triage-control-room/development-report.md`

默认下一步：开发完成后直接进入验收窗口，无需先等待控制室二次派工，除非你发现本轮明显 scope drift 或报告无法支撑独立验收。
