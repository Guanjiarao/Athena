package com.whu.software.athena.cognitionagent.intent.contract;

/** Outcome of the optional model-assistance call. */
public enum ModelCallStatus {
    NOT_ATTEMPTED,
    SUCCEEDED,
    FAILED,
    REJECTED
}
