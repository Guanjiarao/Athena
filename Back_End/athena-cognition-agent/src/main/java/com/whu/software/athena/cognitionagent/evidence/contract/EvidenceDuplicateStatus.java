package com.whu.software.athena.cognitionagent.evidence.contract;

public enum EvidenceDuplicateStatus {
    UNIQUE,
    EXACT_SOURCE_DUPLICATE,
    EXACT_CONTENT_DUPLICATE,
    SOURCE_CONFLICT,
    REJECTED
}
