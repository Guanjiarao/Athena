package com.whu.software.athena.cognitionagent.graph.contract;

/** Kept aligned with the Android cognition action contract. */
public enum GraphActionType {
    RECORD_BODY,
    RECORD_MOOD,
    RECORD_SLEEP,
    READ_CONTENT,
    ANSWER_QUESTION,
    CONFIRM_STATUS
}
