# Athena 软件著作权源代码提交与整理说明

## 一、软件基本情况

### 1.1 软件名称

Athena 软件著作权源代码提交与整理说明。

### 1.2 软件组成

本软件为 Athena 项目，整体由网站端前端、App 用户端前端以及后端服务集群三部分组成。各部分代码职责明确、层次清晰、相互协同，共同构成完整的业务系统。

1. 网站端前端：`admin_web_front`
2. App 用户端前端：`athena_app_front`
3. 后端服务集群：`Back_End`

其中，网站端前端主要面向后台管理人员和内容运营人员，用于内容审核、内容发布、内容管理及系统配置；App 用户端前端主要面向终端用户，用于内容浏览、互动、健康记录及 AI 服务交互；后端服务集群负责统一认证、业务处理、数据流转、文件存储、统计分析以及智能问答能力支撑。上述三部分共同构成 Athena 软件系统的完整技术体系。

### 1.3 整理目的

本说明用于对 Athena 项目代码结构、功能模块、核心文件及模块关系进行规范化整理，以便于软件著作权申请材料的编制、打印、盖章和提交；同时用于配合源代码 Word 成稿的分页整理与装订。

---

## 二、网站端前端代码说明（`admin_web_front`）

### 2.1 软件功能概述

网站端前端用于支撑后台管理业务，主要实现内容审核、内容发布、内容管理、账号登录与权限控制、系统设置及业务面板展示等功能。该部分程序面向管理员和发布人员，承担运营管理与内容流转入口职责，是系统后台管理的重要组成部分。

### 2.2 主要技术特点

网站端前端采用现代化前端技术体系，具备组件化开发、路由控制、状态管理和接口封装等能力，能够满足后台业务系统的交互需求。

- 框架：React 19
- 开发与构建工具：Vite
- 语言：TypeScript
- 路由：React Router DOM
- 网络请求：Axios
- UI 与样式：Tailwind CSS、shadcn 风格组件、Radix UI、Lucide 图标、Framer Motion
- 富文本编辑：React Quill New

### 2.3 主要目录及文件

- `src/main.tsx`：应用入口，挂载 React 根组件
- `src/App.tsx`：路由入口与权限路由控制
- `src/contexts/AuthContext.tsx`：登录态上下文管理
- `src/layouts/MainLayout.tsx`：主布局框架
- `src/pages/`：页面级组件目录
  - `LoginPage.tsx`：登录页
  - `AuditPage.tsx`：审核页
  - `ContentManagePage.tsx`：内容管理页
  - `PublishPage.tsx`：发布页
  - `SettingsPage.tsx`：设置页
- `src/pages/audit/`：审核模块子页面
- `src/pages/publish/`：发布模块子页面
- `src/components/`：通用组件与业务组件
- `src/api/`：后端接口请求封装
- `src/lib/`：公共工具、业务常量与辅助函数

### 2.4 核心功能模块

#### 2.4.1 登录与鉴权模块

网站端通过 `AuthContext` 维护登录状态，并将会话信息保存到本地存储中。`App.tsx` 中通过权限路由实现未登录跳转、角色访问控制及不同页面权限分配。

#### 2.4.2 内容审核模块

审核模块以审核列表和审核面板为核心，面向管理员展示待审核内容、审核状态与处理结果，支持审核流转与结果反馈。

#### 2.4.3 内容发布模块

发布模块支持文章与视频内容发布，并按业务流程提交到审核环节，形成内容生产与审核闭环。

#### 2.4.4 内容管理模块

内容管理模块用于管理已发布内容、查看内容状态以及执行相关编辑操作，保障内容运营的连续性。

#### 2.4.5 系统设置模块

系统设置模块用于账号、偏好和基础配置管理，为后台业务提供统一配置入口。

### 2.5 关键文件说明

- `src/App.tsx`：定义应用路由结构及角色访问控制逻辑
- `src/api/axios.ts`：统一请求实例与 token 注入逻辑
- `src/contexts/AuthContext.tsx`：登录态读写与退出逻辑
- `src/pages/PublishPage.tsx`：发布入口页，整合文章与视频发布能力
- `src/pages/AuditPage.tsx`：审核模块入口页

### 2.6 构建脚本

`admin_web_front/package.json` 中定义了以下常用脚本：

- `npm run dev`：启动开发环境
- `npm run build`：执行 TypeScript 检查并构建生产包
- `npm run lint`：执行代码规范检查
- `npm run preview`：预览构建结果

---

## 三、App 用户端前端代码说明（`athena_app_front`）

### 3.1 软件功能概述

App 用户端前端面向终端用户，主要实现用户登录与注册、内容浏览、搜索、收藏、点赞、评论、内容发布、个人资料管理、社交关注关系管理、健康记录、周期管理、AI 问答、知识推荐、分析报告、隐私处理以及图片上传与本地缓存等功能。

### 3.2 主要技术特点

App 用户端基于 Android 原生技术开发，结合常用组件、网络库、图片加载库与多媒体播放能力，满足移动端业务场景需求。

- 平台：Android 原生应用
- 语言：Java
- 构建工具：Gradle Kotlin DSL
- UI 组件：AndroidX、Material Design、ConstraintLayout、RecyclerView、ViewPager2
- 网络请求：OkHttp、Gson
- 图片加载：Glide
- 视频播放：GSYVideoPlayer
- 文字识别：Google ML Kit
- 其他：Markdown 渲染、健康设备 SDK、文件上传与本地缓存等

### 3.3 主要工程结构

- `app/src/main/java/com/whu/software/athena/`：核心业务代码
  - `MainActivity.java`：主入口活动
  - `LoginActivity.java`、`RegisterActivity.java`：登录注册
  - `KnowledgeFragment.java`、`SquareFragment.java`、`RecordFragment.java`、`ProfileFragment.java`：底部导航主模块
  - `SearchActivity.java`、`SearchResultActivity.java`：搜索相关
  - `ArticleDetailActivity.java`、`NoteDetailActivity.java`、`VideoDetailActivity.java`：内容详情
  - `PublishActivity.java`、`PublishVideoActivity.java`：发布内容
  - `AnalysisReportActivity.java`：分析报告
- `app/src/main/java/com/whu/software/athena/features/`：按业务领域拆分的功能模块
  - `chat`：AI 对话与历史记录
  - `privacy`：隐私分析、脱敏与联邦学习相关处理
  - `monitor`、`triage` 等：专项功能模块
- `app/src/main/java/com/whu/software/athena/utils/`：工具类、网络服务、数据管理、上传与图像处理
- `app/src/main/java/com/whu/software/athena/entity/`：数据实体类
- `app/src/main/res/layout/`：界面布局文件
- `app/src/main/res/drawable/`：图标、背景、状态选择器等资源
- `app/src/main/res/values/`：字符串、主题、颜色与尺寸资源
- `app/src/main/AndroidManifest.xml`：应用组件、权限与入口配置

### 3.4 核心功能模块

#### 3.4.1 启动与首页导航模块

`MainActivity` 作为应用启动入口，通过底部导航在知识、广场、记录、个人中心等模块间切换，并根据本地登录状态控制展示内容。

#### 3.4.2 内容与社区模块

App 端支持文章、视频、笔记、评论、点赞、收藏、关注等社区功能，形成内容浏览与互动闭环。

#### 3.4.3 记录与健康管理模块

记录模块围绕健康数据、周期记录、测量信息、图表展示和历史数据管理展开，服务于用户长期健康管理需求。

#### 3.4.4 AI 与知识服务模块

项目包含 AI 问答、推荐、分析与检索能力，支持用户基于业务场景获取个性化知识与辅助建议。

#### 3.4.5 隐私与安全处理模块

隐私模块包括敏感信息识别、脱敏处理、联邦学习相关交互及隐私内容存储控制，保障用户数据安全。

#### 3.4.6 多媒体与上传模块

支持图片、视频、文件上传，以及本地图片管理和预览，满足多媒体内容的展示与处理需求。

### 3.5 关键文件说明

- `app/src/main/AndroidManifest.xml`：声明应用权限、组件及启动入口
- `app/build.gradle.kts`：Android 构建配置与依赖管理
- `app/src/main/java/com/whu/software/athena/MainActivity.java`：主导航与启动逻辑
- `app/src/main/java/com/whu/software/athena/config/ApiConfig.java`：后端接口地址集中配置
- `app/src/main/java/com/whu/software/athena/features/privacy/RLHFDialogHelper.java`：隐私交互辅助
- `app/src/main/java/com/whu/software/athena/features/chat/MessageAdapter.java`：聊天消息列表适配

### 3.6 构建与运行配置

App 端工程使用 Gradle 进行依赖管理，`app/build.gradle.kts` 中配置了 AndroidX、Material、OkHttp、Gson、Glide、ML Kit、视频播放器和健康设备 SDK 等依赖，适用于 Android 原生移动端开发。

---

## 四、后端代码说明（`Back_End`）

### 4.1 软件功能概述

后端服务集群用于承载 Athena 项目的统一业务处理与数据支撑能力，主要实现用户认证、内容发布与审核、评论互动、点赞收藏、浏览记录、统计计数、排行榜、健康记录、分析报告、推荐洞察、文件存储以及 RAG 知识问答等功能，是系统运行的核心支撑部分。

### 4.2 主要技术特点

后端采用分布式多模块架构设计，结合服务治理、缓存、消息队列、关系型数据库与向量检索能力，支持业务解耦和扩展部署。

- 基础语言：Java
- 构建工具：Maven
- 服务框架：Spring Boot、Spring Cloud
- 数据访问：MyBatis-Plus、MyBatis、JDBC
- 服务治理与通信：OpenFeign、Gateway、Nacos 风格配置、统一上下文透传
- 缓存与消息：Redis、RocketMQ
- 数据库：MySQL、PostgreSQL、Milvus、向量库与关系型数据库协同
- 对象存储：OSS / S3 风格存储
- AI 与检索：RAG、Embedding、LLM、Rerank、MCP、流式响应
- 其他能力：定时任务、分布式锁、幂等控制、文件解析、文档切分、分词与召回

### 4.3 后端总体目录结构

`Back_End` 目录下采用多模块 Maven 工程组织，不同模块职责边界清晰，并通过 RPC、消息队列和公共基础设施模块协同工作。

- `athena-gateway`：统一网关入口，负责请求转发、鉴权拦截与用户标识透传
- `athena-framework`：后端基础能力抽象，包括通用工具、过滤器、消息封装与公共配置
- `athena-count`：计数服务，负责点赞、收藏、评论、浏览等计数聚合
- `athena-comment`：评论服务，负责评论发布、回复、点赞和评论结构管理
- `athena-ground`：内容主业务服务，负责帖子/笔记内容发布、审核、互动与 RAG 同步
- `athena-oss`：文件存储服务，负责对象存储上传与下载
- `athena-userauth`：用户认证服务，负责登录、注册、用户信息与鉴权数据
- `athena-relation`：用户关注关系服务，负责关注、粉丝和关系维护
- `athena-record`：健康记录服务，负责日常记录、周期管理和统计展示
- `athena-rank`：排行榜与数字资产相关服务，负责排名策略与指标聚合
- `athena-insight`：用户洞察与推荐服务，负责特征分析、推荐、报告和主题建模
- `athena-rag`：RAG 与 AI 问答服务，负责知识库、检索、对话、意图识别、预分诊及后台管理

### 4.4 模块之间的逻辑关系

后端各模块围绕“用户内容生产—审核发布—互动增长—数据沉淀—智能分析—AI 问答”形成完整闭环。

1. `athena-gateway` 作为统一入口，接收前端请求并透传用户身份到后端各服务。
2. `athena-userauth` 提供登录鉴权能力，是其他业务模块的用户身份基础。
3. `athena-ground` 负责内容主流程，内容创建后可流转到审核、发布、互动与同步环节。
4. `athena-comment`、`athena-count`、`athena-relation` 提供社区互动支撑能力，并与内容模块联动。
5. `athena-record`、`athena-rank`、`athena-insight` 对业务数据进行沉淀、计算、分析和推荐。
6. `athena-oss` 为图片、视频、文档等资源提供统一存储入口。
7. `athena-rag` 与 `athena-ground`、`athena-insight` 等模块协同，将内容、知识和问答能力串联起来，支持 AI 检索与智能推荐。
8. `athena-framework` 提供消息、过滤器、工具类、上下文传递等公共支撑能力，供各业务模块复用。

### 4.5 主要功能模块说明

#### 4.5.1 网关与公共基础设施模块

- `athena-gateway`：统一接入层，处理路由、认证前置拦截和请求头透传。
- `athena-framework-basic`：提供公共常量、结果封装、正则工具、JSON 工具、用户上下文等基础能力。
- `athena-framework-filter`：提供请求上下文与 Feign 透传过滤能力。
- `athena-framework-mq`：提供消息队列抽象、生产者封装与自动配置。

#### 4.5.2 用户认证与关系链模块

- `athena-userauth`：实现用户注册、登录、用户资料、会话鉴权与 Feign API 暴露。
- `athena-relation`：实现关注、粉丝、关系查询与维护，供社区功能使用。

#### 4.5.3 内容主业务与互动模块

- `athena-ground`：负责笔记/帖子发布、审核、互动、RAG 同步、搜索与内容审核流转。
- `athena-comment`：负责评论的发布、回复、点赞及评论树结构。
- `athena-count`：负责对内容互动行为进行计数聚合，减少主业务表压力。
- `athena-oss`：为内容中的图片、附件和媒体资源提供上传能力。

#### 4.5.4 健康记录、排行榜与洞察模块

- `athena-record`：负责健康记录、周期记录、统计与趋势查询。
- `athena-rank`：提供排行榜、数字资产和多场景排名策略。
- `athena-insight`：基于记录、内容和用户特征进行推荐、报告生成与主题分析。

#### 4.5.5 RAG 与智能问答模块

- `athena-rag`：提供知识库管理、文档上传解析、切分入库、向量检索、意图识别、上下文记忆、流式问答、预分诊、后台管理与评测能力。
- 该模块内部可进一步划分为：
  - `bootstrap`：业务启动层与接口层
  - `core`：解析、切分、检索、Prompt、记忆、意图等核心能力
  - `framework`：Web、MQ、分布式 ID、幂等、异常与公共基础设施
  - `infra-ai`：模型调用、embedding、rerank、流式对话等 AI 基础能力
  - `ingestion`：文档接入、解析、增强、索引和流水线
  - `knowledge`：知识库、文档、分块、定时同步与消息驱动
  - `triage`：预分诊、问诊、风险判断与对话编排
  - `mcp-server`：MCP 工具服务
  - `frontend`：RAG 管理后台前端

### 4.6 关键文件与入口说明

- `Back_End/pom.xml`：后端父工程，统一管理多模块依赖与构建
- `athena-gateway/src/main/java/.../AthenaGatewayApplication.java`：网关启动入口
- `athena-framework/*`：后端公共基础设施入口与自动配置
- `athena-userauth/athena-userauth-biz/src/main/java/.../BackEndApplication.java`：用户认证服务入口
- `athena-ground/athena-ground-biz/src/main/java/.../AthenaGroundApplication.java`：内容主业务入口
- `athena-comment/athena-comment-biz/src/main/java/.../AthenaCommentBizApplication.java`：评论服务入口
- `athena-count/athena-count-biz/src/main/java/.../AthenaCountApplication.java`：计数服务入口
- `athena-record/athena-record-biz/src/main/java/.../AthenaRecordBizApplication.java`：记录服务入口
- `athena-insight/athena-insight-biz/src/main/java/.../AthenaInsightApplication.java`：洞察服务入口
- `athena-rag/bootstrap/src/main/java/.../RagentApplication.java`：RAG 服务入口

### 4.7 构建与运行特征

后端整体采用 Maven 多模块方式构建，各业务模块独立打包、独立部署，同时通过 API 模块、Feign 接口、消息队列和公共基础设施实现协同。配置文件通常集中在 `src/main/resources/config/` 下，采用 `bootstrap.yml`、`bootstrap-prod.yml` 等方式区分环境。部分模块包含 `target/classes` 下的构建产物，说明工程已经完成本地构建验证。

---

## 五、系统整体结构及协同关系说明

### 5.1 整体结构

Athena 软件系统由网站端前端、App 用户端前端和后端服务集群共同构成，分别承担管理端操作、用户端交互和后端业务支撑职责。

### 5.2 协同关系

1. 网站端前端负责内容审核、内容发布和后台管理，通过后端接口完成业务操作。
2. App 用户端前端负责用户内容消费、互动操作、健康记录和 AI 服务调用，通过后端接口获取业务数据。
3. 后端服务集群负责统一认证、业务流转、数据持久化、统计分析和智能能力输出。
4. 三端通过网关、接口调用、消息机制和数据同步实现协同，形成完整业务闭环。

### 5.3 业务闭环

系统业务链路可概括为：内容生产、审核发布、用户浏览、互动反馈、数据沉淀、统计分析、知识检索、智能问答。各模块之间分工明确、衔接顺畅，能够支撑 Athena 软件的整体运行。

---

## 六、软件著作权材料整理建议

### 6.1 文档使用建议

本说明适合直接转换为 Word 文档后打印、签章和提交。建议在 Word 中保持统一的标题层级、页边距、段前段后间距以及统一字体，以保证材料规范整洁。

### 6.2 提交材料建议

1. 建议在软著材料中统一命名：
   - 网站端前端：`admin_web_front`
   - App 用户端前端：`athena_app_front`
   - 后端：`Back_End`
2. 若需要提交源代码目录说明，可按“项目概述 + 总体结构 + 模块说明 + 关键文件 + 运行配置 + 三端关系”组织。
3. 涉及外部接口地址、密钥、真实服务器信息的内容，建议在软著提交材料中做脱敏处理。
4. 建议保留各模块入口文件、接口层、核心业务层、公共基础设施、资源配置文件和构建配置文件，以体现程序完整性。
5. 后端材料建议突出模块之间的协作链路，尤其是“内容主业务—互动计数—关系链—洞察分析—RAG 问答”的逻辑闭环。

---

## 七、结论

Athena 软件系统由网站端前端 `admin_web_front`、App 用户端前端 `athena_app_front` 和后端 `Back_End` 三部分组成。三部分代码结构清晰、职责明确、逻辑衔接完整，能够充分体现软件的整体功能、模块划分和技术实现特征。若用于软件著作权正式申报，建议配合《Athena 软件著作权源代码提交稿_正文版.md》一并整理为 Word 文档后打印、装订和提交。
